{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the single Guac window. Guac talks to the network only from Rust, so the webview gets no HTTP or filesystem access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "opener:allow-open-url"
  ]
}
